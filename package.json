{
  "name": "showroom-ai-quickstart-it-self-service-agent",
  "private": true,
  "description": "Local Antora only: Podman viewer mounts this repo and resolves @sntke/antora-mermaid-extension from node_modules. CI installs the extension globally instead.",
  "dependencies": {
    "@sntke/antora-mermaid-extension": "0.0.9"
  }
}
